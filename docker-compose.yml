services:
  backend:
    build: .
    env_file: backend/.env
    volumes:
      - agent_db:/app/backend
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  agent_db:
